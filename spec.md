# ModelScope Agent Spec（目标与约束）

## 1. 目标

1. 构建一个面向 ModelScope 场景的 RAG Agent，支持文档检索、重写、重排、生成与评测闭环。
2. 保持主链路可持续运行：抓取、索引、问答、评测四条链路具备可执行基线。
3. 通过模块化和可测试设计降低维护成本，支持后续多模态增强迭代。

## 2. 需求范围

1. 运行链路：`crawl.py`、`build.py`、`app.py`、`scripts/run_eval.py`。
2. 核心模块：`src/graph.py`、`src/node/*`、`src/eval/*`、`src/llm/*`。
3. 数据资产：`data/raw`、`data/faiss_db`、`data/eval/datasets`、`data/eval/reports`。

## 3. 关键约束

1. 环境与命令约束：统一使用 `uv`，执行口径为 `uv run ...`。
2. 架构约束：
	- 图编排与路由/业务决策分离。
	- 持久化逻辑与 UI 事件分离。
	- 配置与执行分离，优先可测试组件。
3. 文档约束（Specscoding）：
	- `spec.md` 仅保留目标、需求范围、约束与验收标准。
	- `plan.md` 仅保留整体计划、阶段策略与里程碑。
	- `task.md` 仅保留任务分解、负责人（可选）与进度状态。
	- `CHANGELOG.md` 仅保留可追溯变更记录。
4. 语言约束：项目文档以中文为主，必要时可附英文补充。

## 4. 开发流程约束（保留 TDD）

1. Red：先写失败测试，先定义行为与验收。
2. Green：实现最小改动使测试通过。
3. Refactor：在测试通过前提下重构，保持行为稳定。
4. 每个功能变更应至少具备一种可验证手段（单测或关键命令）。

## 5. 验收约束

1. 文档治理验收：四类文档分层清晰，不交叉堆叠。
2. 工程验收：关键命令可执行，且不引入明显回归。
3. 版本验收：变更可在 `CHANGELOG.md` 中映射到具体文件或行为。

## 6. 非目标

1. 本轮不引入新的视觉模型推理链路。
2. 本轮不扩展与当前目标无关的 UI/部署体系重构。
