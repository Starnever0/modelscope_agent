# ModelScope Agent Plan（整体计划）

## 1. 计划原则

1. 以 `spec.md` 作为唯一目标与约束来源。
2. 以 TDD 作为默认研发范式，先测后改。
3. 每轮迭代遵循“计划 -> 执行 -> 回写 CHANGELOG -> 提交”的闭环。

## 2. 阶段规划

### Phase A：基线稳定

1. 稳定抓取、索引、问答、评测四条主链路。
2. 保持模型配置、数据读取、评测输入输出的一致性。

### Phase B：评测治理

1. 完善评测数据集构建工具与输入兼容策略。
2. 统一检索指标、时延指标、生成质量指标的口径。
3. 支持 LangSmith 上传与追溯分析。

### Phase C：文档与流程治理

1. 落地 Specscoding 文档分层：`spec.md`、`plan.md`、`task.md`、`CHANGELOG.md`。
2. 统一 Skill 工作流，明确开发前后门禁与提交规范。
3. README 与领域指南按模块同步维护。

### Phase D：能力扩展（滚动）

1. 在不破坏文本 RAG 的前提下，推进图片元数据与多模态增强。
2. 对新增能力继续采用 TDD 与阶段验收策略。

## 3. 里程碑

1. M1：文档分层改造完成，流程规则统一。
2. M2：评测数据链路与脚本稳定，结果可复现。
3. M3：多模态增强进入可控试运行。

## 4. 风险与应对

1. 风险：本地环境不一致导致验证偏差。
   应对：统一使用 `uv run`，在文档中显式给出命令。
2. 风险：文档与代码演进不同步。
   应对：每次提交前强制检查 `task.md` 与 `CHANGELOG.md`。
3. 风险：外部模型/网络波动影响评测稳定性。
   应对：保留降级路径，并在变更日志中标注外部阻塞。
